import React from "react";

interface ResProps {
  header: string;
  url: string;
  reqRes: any;
  subHead: string;
}

const MediumRes = ({ header, reqRes, url, subHead }: ResProps) => {
  return <div>MediumRes</div>;
};

export default MediumRes;
